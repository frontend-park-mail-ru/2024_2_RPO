import { ComponentProps } from '@/jsxCore/types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const TagSettings = (props: ComponentProps) => {
  return <div>Здесь будет пизда!</div>;
};
